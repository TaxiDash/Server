<%= render 'layouts/header' %>

<div class="container-fluid">
  <div class="row">
        <%= render 'layouts/sidebar' %>
    <div class="col-md-8 col-sm-10 col-md-offset-3 col-sm-offset-2 main">
      <h1 class="page-header"><%= yield(:heading) %></h1>
      <%= yield %>            
    </div>
  </div>
</div>
