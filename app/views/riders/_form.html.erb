<%= form_for(@rider) do |f| %>
  <% if @rider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rider.errors.count, "error") %> prohibited this rider from being saved:</h2>

      <ul>
      <% @rider.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-horizontal">
    <div class="form-group">
      <%= f.label :uuid, :class => "col-sm-3 control-label" %>
      <div class="col-sm-5"><%= f.text_field :uuid, :class => "form-control", :autofocus => true %></div>
    </div>

  <div class="col-sm-offset-3 col-sm-7">
      <%= f.submit "Submit", :class=>"btn btn-primary" %>
  </div>
  </div>

<% end %>
