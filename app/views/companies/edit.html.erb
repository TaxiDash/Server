<% provide(:title, 'Edit Company') %>
<%= content_for :heading do %>
    <%= render 'layouts/buttons/back_button' %>
    <%= link_to(show_company_path(@company), :class => "btn btn-lg btn-link pull-right", title: "Edit", method: :get) do %>            
        <span class="glyphicon glyphicon-eye-open"></span> Show
    <% end %>
    <%= @company.name %> 
<% end %>

<%= render 'form' %>
