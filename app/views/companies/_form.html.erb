<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-horizontal">
    <div class="form-group">
      <%= f.label :name, :class => "col-sm-3 control-label" %>
      <div class="col-sm-5"><%= f.text_field :name, :class => "form-control", :autofocus => true %></div>
    </div>
    <div class="form-group">
      <%= f.label :phone_number, :class => "col-sm-3 control-label" %>
      <div class="col-sm-5"><%= f.text_field :phone_number, :class => "form-control" %></div>
    </div>
    <div class="form-group">
      <%= f.label :logo, :class => "col-sm-3 control-label" %>
      <div class="col-sm-5"><%= f.file_field :logo%></div>
    </div>

  <div class="col-sm-offset-3 col-sm-7">
      <%= f.submit "Submit", :class=>"btn btn-primary" %>
  </div>
  </div>
<% end %>
