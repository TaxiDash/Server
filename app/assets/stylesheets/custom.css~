@import "bootstrap";

body {
  padding-top: 60px;
}

.navbar-brand {
  font-size: 1.7em;
  color: #fff;
  letter-spacing: -1px;
  padding-top: 9px;
  font-weight: bold;
}

.brand:hover {
  color: #fff;
  text-decoration: none;
}
